name: Create release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Install zip
        run: sudo apt install zip

      - name: Get latest code
        uses: actions/checkout@v4

      - name: Bundle files
        run: |
          mkdir -p ./temp/pulsar-dealerships
          cp ./{README.md,fxmanifest.lua,native_dlc.json,sandbox_rp_dealership_vehicles.json} ./temp/pulsar-dealerships
          cp -r ./{client,server,config,shared} ./temp/pulsar-dealerships
          cd ./temp && zip -r ../pulsar-dealerships.zip ./pulsar-dealerships

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "PULSAR-DEALERSHIPS ${{ github.ref_name }}"
          body: |
            ![Pulsar Framework](https://r2.fivemanage.com/GPYOH8Hq4GPyAY7czrgLe/pulsarbanner.png)

            ## PULSAR-DEALERSHIPS ${{ github.ref_name }}

            Vehicle dealerships — sales, showrooms, test drives, and rentals for Pulsar Framework.

            ### Installation
            1. Download `pulsar-dealerships.zip` below
            2. Extract into your server's `resources` folder
            3. Add `ensure pulsar-dealerships` to your `server.cfg`

            ---
            ![Pulsar Framework](https://img.shields.io/badge/Pulsar-Framework-7c3aed?style=flat-square)
            ![Built for FiveM](https://img.shields.io/badge/Built_for-FiveM-F40552?style=flat-square)
          files: pulsar-dealerships.zip
          prerelease: false
